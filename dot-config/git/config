[user]
        name = Peter Vlk
        email = petervlk86@gmail.com

[core]
        autocrlf = input
        editor = nvim

[fetch]
        prune = true
	
[diff]
        tool = nvimdiff

[merge]
        tool = nvimdiff
        conflictstyle = zdiff3

[mergetool]
        prompt = false
        keepBackup = false

[mergetool "nvimdiff"]
        layout = "LOCAL,BASE,REMOTE / MERGED"

## REuse REcorded REsolution
#
# remember how merge conflicts were resolved
# example: resolve conflicts during rebase
[rerere]
	enabled = true

[url "ssh://git@github.com/"]
        insteadOf = https://github.com/

[includeIf "gitdir:~/projects/work/flexiana/gravie/"]
    path = ~/.config/git/gitconfig-gravie
